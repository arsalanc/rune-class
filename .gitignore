node_modules/
server/players/
server/world.json
*.log
# dev-only: holds the npm packages used to regenerate js/vendor/ (see tools/build-vendor.sh).
# The generated bundles in js/vendor/ ARE committed — the game must run from a plain
# static host with no build step.
.tooling/
# Development screenshots. Nothing in the game references them; they were 15 MB of a
# 16 MB repo, so they stay on disk but out of git.
reference/
