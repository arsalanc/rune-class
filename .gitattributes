# Normalise to LF in the repository. Without this, a Windows checkout gives
# tools/build-vendor.sh CRLF endings and `sh` fails on the stray \r.
* text=auto eol=lf

# The vendored bundles are generated; keep them out of diffs and language stats.
js/vendor/* linguist-vendored
